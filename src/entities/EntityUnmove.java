package entities;

import javafx.scene.image.Image;

public abstract class EntityUnmove extends Entity{
    public EntityUnmove(int xUnit, int yUnit, Image img) {
        super(xUnit, yUnit, img);
    }

}
